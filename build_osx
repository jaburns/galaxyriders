#!/usr/bin/env bash
set -e
cd "$(dirname "${BASH_SOURCE[0]}")"

cc -o game \
        src/*.cpp \
        src/models/*.cpp \
        external/lodepng/lodepng.cpp \
    -Iexternal/lodepng \
    -Iexternal/glm \
    -std=c++14 \
    -lstdc++ \
    -lglfw \
    -framework OpenGL

./game
