#!/usr/bin/env bash
set -e
cd "$(dirname "${BASH_SOURCE[0]}")"

cc -o game \
        src/*.cc \
        src/models/*.cc \
        external/lodepng/*.cc \
    -Iexternal/lodepng \
    -Iexternal/glm \
    -std=c++14 \
    -lstdc++ \
    -lglfw \
    -framework OpenGL

./game
