#pragma once

#include <cstdint>

namespace Config
{
    static const float MILLIS_PER_TICK = 33.333333f;

    static const uint32_t MAX_PACKET_SIZE = 2048;
    static const uint16_t DEFAULT_PORT = 12345;
}
