#pragma once

// The ImGUI SDL+OpenGL example in their repository includes <GL/gl3w.h>
// We just include our GL header here instead, so we can leave ImGUI unmodified.
#include "../../../src/client/gl.hpp"