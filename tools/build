#!/usr/bin/env bash
set -e
cd "$(dirname "${BASH_SOURCE[0]}")"

mkdir -p ../src/client/gen

g++ -o tools \
        *.cpp \
        ../external/lodepng/lodepng.cpp \
    -O3 \
    -I../external/lodepng \
    -std=c++11

[[ "$1" == 'run' ]] && ./tools
