#!/usr/bin/env bash
set -e
cd "$(dirname "${BASH_SOURCE[0]}")"

mkdir -p ../src/client/gen

OPTIMIZE='-O3'

g++ -o tools \
        *.cpp \
        ../external/lodepng/lodepng.cpp \
    $OPTIMIZE \
    -I../external/lodepng \
    -std=c++11
